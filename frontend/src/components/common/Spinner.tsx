// SPDX-License-Identifier: AGPL-3.0-only
/** Small CSS spinner used inside busy buttons (see .spin in product.css). */
export function Spinner() {
  return <span className="spin" aria-hidden="true" />;
}
